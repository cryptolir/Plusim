<div dir="rtl">

# מדריך למשתמש — Plusim

מדריך זה מסביר כיצד לעבוד עם מערכת הדוחות של Plusim. הוא מחולק לשני חלקים:

- **[חלק א' — למנהלים](#חלק-א--למנהלים-admins)** — יצירת דוחות, הרצתם, בדיקתם ופרסומם דרך ממשק הניהול.
- **[חלק ב' — למשתמשי קצה](#חלק-ב--למשתמשי-קצה-end-users)** — צפייה בדוח, הבנת מבנה הקובץ ומילוי החלקים הידניים.

> **מונחים:** *ג'וב (Job)* = בקשת עיבוד דוח אחת. *סוכן (onlyclaw)* = הסוכן שמסווג את התנועות ובונה את הקובץ. *דף חשבון (Statement)* = קובץ אשראי (Isracard/Leumi בפורמט xlsx, או MAX בפורמט PDF).

---

## חלק א' — למנהלים (Admins)

### 1. מהו צינור הדוחות

מנהל מעלה דפי חשבון אשראי → הסוכן `onlyclaw` מסווג את התנועות לתקציב המשפחתי (טקסונומיית הקטגוריות) → נוצר קובץ Excel עם גיליון לכל חודש וגיליונות ניתוח → המנהל בודק, מאשר ומפרסם → המשתמש רואה את הדוח ב־`/report`.

**עיקרון מנחה:** שום דבר לא נבדק פעם אחת בלבד. הסוכן מאמת את הסכומים לפני השליחה, והמערכת מאמתת שוב באופן עצמאי מהתנועות הגולמיות. אי־התאמה כלשהי → הג'וב מסומן `needs_review` (דורש בדיקה), לעולם לא דוח חלקי שקט.

### 2. יצירת דוח חדש

1. היכנס לממשק הניהול → **Reports** (דוחות).
2. בחר **משתמש יעד** (חייב להיות לו כבר תיקיית Google Drive מוקצית).
3. הוסף **כותרת** (אופציונלי).
4. העלה את דפי החשבון: עד **12 קבצים**, עד **10MB** לקובץ, בפורמט `.xlsx` או `.pdf`.
5. שלח את הטופס. הקבצים הגולמיים נשמרים בתיקיית ה־Drive של המשתמש, ונוצר ג'וב.

### 3. הרצת הג'וב ומעקב אחר הסטטוס

- בדף פרטי הג'וב לחץ **Send to agent** (שלח לסוכן). הפעולה מייצרת אסימון חד־פעמי לג'וב ושולחת לסוכן הודעה קצרה עם קישור ל־manifest.
- הדף מתעדכן אוטומטית כל 10 שניות בזמן שהסטטוס הוא `processing`.
- אם הלקוח (הדפדפן) עושה timeout — אין בעיה: הריצה ממשיכה בצד הסוכן, וה־callback (ולא תשובת הצ'אט) הוא שמסיים את הג'וב. תמיד אפשר ללחוץ **Re-run** (הרצה מחדש) — זה בטוח.

### 4. בדיקת הדוח שנוצר

המטרה: לוודא שהקובץ שנוצר תקין ומכיל את כל תת־הדוחות.

1. **פתיחת ג'וב:** בממשק הניהול → **Reports** → העלה דף חשבון → לחץ **Send to agent** → המתן שהג'וב יגיע לסטטוס **completed** (הושלם), עם התאמת סכומים לכל מקור ✓ (מדויק עד האגורה).

2. **הורדת הקובץ:** הורד את קובץ ה־`.xlsx`. **הסימן החד־משמעי שהכול תקין הוא לשוניות הגיליונות** — צריכות להופיע, לפי הסדר הזה:

   `Main` ← גיליונות החודשים (בתחתית כל אחד **בלוק הכנסות**: הכנסות / הוצאות / זכות-חובה) ← **ניתוח תוצאות** ← **התפלגות ההוצאות** (עם תרשים העוגה) ← **טופס עזר למיפוי** ← **חישוב יעדים** ← `un_categorized` ← `פירוט תנועות`

   אם מופיע רק הסט הישן (Main / חודשים / un_categorized / פירוט תנועות) — גרסת הסוכן לא עודכנה; פנה למי שאחראי על תחזוקת הסוכן.

3. **בדיקת נוסחאות:** פתח את הקובץ ב־Excel / LibreOffice / Google Sheets → ודא ש**אין** שגיאות `#REF!` / `#DIV/0!` / `#NAME?` בשום מקום, ושהמספרים ב"ניתוח תוצאות" שווים לגיליונות החודשים שהם מפנים אליהם.

4. **פרסום ובדיקת ההמרה:** לחץ **Publish** (פרסם) על הג'וב → פתח את ה־Google Sheet שיוצא → ודא שתרשים העוגה והנוסחאות בין־הגיליונות שרדו את ההמרה מ־xlsx ל־Sheets. (אם התרשים נעלם — הטבלה עדיין מכילה את הנתונים; זו מגבלת ההמרה הידועה של Drive, לא באג.)

**צ'קליסט מהיר:**

- ☑️ הג'וב הגיע ל־**completed** (לא `needs_review`).
- ☑️ **10 לשוניות** בקובץ, כולל ארבע החדשות: ניתוח תוצאות, התפלגות ההוצאות, טופס עזר למיפוי, חישוב יעדים.
- ☑️ בלוק ה**הכנסות** קיים בתחתית כל גיליון חודשי.
- ☑️ **אין שגיאות נוסחה** (`#REF!` / `#DIV/0!` / `#NAME?`).
- ☑️ המספרים ב"ניתוח תוצאות" תואמים לגיליונות החודשים.
- ☑️ ב־Google Sheet שפורסם — תרשים העוגה מוצג והנוסחאות עובדות.

### 5. סיווג ידני ואישור מיפויים

- **תנועות ללא סיווג:** בדף פרטי הג'וב מופיעות התנועות שהסוכן לא הצליח לסווג בוודאות. הקצה להן קטגוריה מהטקסונומיה. אפשר לסמן **"זכור בית עסק"** כדי שהסיווג ייכנס למילון הקבוע (`MerchantMapping`) ויחול על כל הג'ובים הבאים.
- **מיפויים שהסוכן הציע:** הסוכן מציע מיפויים חדשים לבתי עסק חוזרים. אשר או דחה אותם — אישור מוסיף אותם למילון ומקטין את זנב הסיווג הידני בעתיד.

### 6. פרסום הדוח

- פרסום מותר כאשר הסטטוס הוא `completed` / `needs_review` / `published`.
- הפרסום **נחסם** אם קיימות בעיות אימות קריטיות (אי־התאמת סכום למקור, קטגוריה לא מוכרת, תאריך מחוץ לחודש, כפילות, קובץ לא־xlsx).
- תנועות שנותרו ללא סיווג **אינן** חוסמות פרסום (הן נספרות ומוצגות).
- הפרסום מייצא את הקובץ ל־Google Sheet בתיקיית ה־Drive של המשתמש, ומאפס את אסימון הג'וב כך שקריאת callback מאוחרת לא תוכל לשנות דוח שכבר פורסם.

### 7. כללי סיווג (report_rules)

בממשק הניהול → **Settings** (הגדרות) ניתן לערוך את `report_rules` — הנחיות סיווג חופשיות שמנחות את **שיקול הדעת של הסוכן לגבי בתי עסק לא־מזוהים בלבד**. הן אינן דורסות סיווג שכבר בוצע במעבר הדטרמיניסטי, ואינן דורסות מיפוי קבוע במילון (שרץ ראשון ומנצח). ריק ⇒ ברירת המחדל (חוקי היסוד) בלבד. השינוי נכנס לתוקף בג'וב הבא.

---

## חלק ב' — למשתמשי קצה (End Users)

### 1. איפה רואים את הדוח

הדוחות שפורסמו מוצגים בעמוד **`/report`**. כל דוח כולל:

- **טבלאות חודשיות** בעברית (מימין לשמאל) — סיכום ההוצאות לפי קטגוריה.
- **הורדת קובץ Excel** — הקובץ המלא עם כל גיליונות הניתוח.
- **קישור ל־Google Sheet** — גרסה מקוונת שאפשר לפתוח ולערוך.

רואים רק דוחות שפורסמו, ורק את הדוחות שלך.

### 2. מבנה הקובץ — הגיליונות

| גיליון | מה יש בו |
|---|---|
| **גיליון לכל חודש** (למשל `יוני 2026`) | ההוצאות של אותו חודש לפי קטגוריות; בתחתית — **בלוק הכנסות** למילוי ידני (משכורות + הכנסות נוספות) ושורת **זכות/חובה**. |
| **ניתוח תוצאות** | השוואה חודש־מול־חודש לכל קטגוריה, עם עמודת **ממוצע**, השוואת הכנסות, וסיכום **"מצבנו"** (הכנסות מול הוצאות ויתרה). |
| **התפלגות ההוצאות** | טבלת ממוצעים לפי קטגוריה + **תרשים עוגה** של חלוקת ההוצאות. |
| **טופס עזר למיפוי** | טופס עזר לייעוץ (חברות ביטוח, הלוואות, כרטיסי אשראי) — למילוי ידני. |
| **חישוב יעדים** | מחשבון חיסכון לילדים — הכנס סכום יעד, גיל יעד וגיל הילד, וקבל את החיסכון החודשי הדרוש. |
| **un_categorized** | תנועות שלא סווגו, עם הסיבה. |
| **פירוט תנועות** | פירוט מלא של כל התנועות והסיווג שלהן. |

### 3. החלקים שממלאים ידנית

חלק מהגיליונות הם **שלד עם נוסחאות חיות** שממתין למילוי — בדיוק כמו בתבנית הידנית של היועץ:

- **בלוק ההכנסות** (בתחתית כל גיליון חודשי): הקלד את המשכורות וההכנסות הנוספות בתאים הריקים. הסיכומים, שורת ה"זכות/חובה" וגיליון "ניתוח תוצאות" מתעדכנים אוטומטית.
- **חישוב יעדים:** מלא סכום יעד (`D1`), גיל יעד (`F1`), ולכל ילד — שם וגיל. שורות ריקות נשארות ריקות ואינן משפיעות על הסיכום.
- **טופס עזר למיפוי:** מלא את פרטי הביטוחים, ההלוואות וכרטיסי האשראי — לשימוש בפגישת הייעוץ.

> כל הסכומים בשקלים, והנוסחאות חיות — אפשר לערוך את הקובץ ידנית והכול יתחשב מחדש.

### 4. צ'אט עם היועץ

בעמוד הצ'אט אפשר לשוחח עם היועץ (הסוכן) ולשאול שאלות על הדוח והתקציב. השיחה מתבצעת דרך המערכת של Plusim.

---

*מסמך זה מתוחזק ב־`USER_GUIDE.md` שבמאגר. לפרטים טכניים על צינור הדוחות ראו `docs/REPORTS_PIPELINE.md`.*

</div>
